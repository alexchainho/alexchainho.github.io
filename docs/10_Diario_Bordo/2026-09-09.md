# Diario de Bordo — Portfolio

**Data:** 2026-09-09

## Sessão (aberta sem `oc`, diário criado manualmente a posteriori)

### Trabalho feito

- Verificação de conformidade do projecto com as regras globais
  (`~/.config/opencode/AGENTS.md`): revistos git, `docs/tasks.md`,
  `docs/lessons.md`, SDD (`01-requisitos/`), diário de bordo e o código novo
  (`tools/export-pdf.mjs`, comparado com RNF-001).
- Encontrados 3 desvios: falta da junction Obsidian, falta de `AGENTS.md` na
  raiz do projecto, estrutura `docs/` incompleta face ao template
  (`00_Planeamento/`, `20_Conceitos_Tecnicos/`, `30_Documentacao_Projeto/`,
  `40_Problemas_Solucoes/`, `00_INDICE_Portfolio.md`).
- Corrigidos os 3 desvios: criada a junction
  `F:\documentos\Pessoal\Projetos\Portfolio` → `docs/`; criado `AGENTS.md` na
  raiz (regras específicas do projecto); criadas as pastas em falta e
  `docs/00_INDICE_Portfolio.md`.
- Commit `7549c45` (`docs: adicionar AGENTS.md e indice do projecto`) e push
  para o remoto.
- Esclarecido o mecanismo de hooks OpenCode: o diário do dia e o
  `graphify update` só correm automaticamente quando a sessão arranca com o
  alias `oc` (função `opencode-session` no `$PROFILE`), que embrulha
  `opencode` e chama `sessao-inicio.ps1`/`sessao-fim.ps1`. Esta sessão
  arrancou sem `oc`, por isso o diário de hoje não foi criado sozinho — feito
  manualmente aqui. Nota: este projecto não tem `graphify-out/graph.json`,
  por ser site estático sem grafo de código configurado.

## Sessao 18:05

### Trabalho feito

20:21 — Reposicionamento do perfil Fiverr a partir do portefólio.

- Extraídos os dados públicos do perfil `alexchainho` (bloco
  `perseus-initial-props` da página, via WebFetch em HTML + análise delegada a
  sub-agente por o ficheiro ter 1,2 MB). Resultado contraria a percepção
  inicial: **0 gigs aprovados**, 0 reviews, 0 portfolio, 0 work experience, e
  `isActivationCompleted: false` — a activação como vendedor nunca foi
  concluída, apesar da conta existir desde Fev/2023.
- Análise de concorrência com pesquisas na Fiverr (`python automation`,
  `gis mapping`, `arcgis`, `geospatial python automation`). Conclusão: a
  categoria Geographic Information Systems tem 1.300–1.600 gigs contra 21.000
  em Software Development e 12.000 em Automations & Agents. Decisão: posicionar
  na **intersecção GIS + automação Python**, não em nenhum dos dois isolados.
  Evidência: nesse nicho, 8 reviews chegam para o badge "Fiverr's Choice";
  em automação Python genérica um Level 2 precisa de 30–50.
- Escrito `docs/50_Fiverr/Perfil_e_Gigs.md`: diagnóstico, análise de mercado,
  About me em inglês dentro do limite de 600 caracteres, 3 gigs completos
  (título, tags, 3 pacotes com preços, descrição, FAQ, requisitos ao
  comprador), correcção das 8 skills declaradas, 6 entradas de Portfolio e 2
  de Work Experience.
- Duas ressalvas registadas no documento: (1) links externos na descrição do
  perfil são zona cinzenta nos ToS da Fiverr — não incluir o link do
  portefólio; (2) o texto está escrito **sem nomear a GNR/UEPS**, com versão
  alternativa que nomeia, para decisão do utilizador — vender serviços com base
  em trabalho de uma força de segurança é contexto diferente de um portefólio
  pessoal público.
- Nota de método: o alias `oc` funcionou nesta sessão (secção aberta às 18:05
  pelo hook), ao contrário da sessão anterior do mesmo dia.

20:40 — Correcção ao diagnóstico da Fiverr.

- O utilizador mostrou o painel de vendedor: existem **4 gigs pausados**
  (`create excel sheets`, `deliver high quality transcriptions`,
  `create geographical contours and elevations`, `review your python code`),
  criados no arranque da conta.
- Gigs pausados não aparecem na API pública nem contam para
  `approvedGigsCount` — daí o JSON ter dado `gigs.nodes: []`. A minha conclusão
  anterior ("nunca teve gigs publicados") estava errada: enumerei "rascunho ou
  recusado" e falhei o estado real. Erro registado em `docs/lessons.md` —
  confundir *ausência de evidência* com *evidência de ausência*.
- Secção 1 de `docs/50_Fiverr/Perfil_e_Gigs.md` reescrita com a tabela dos 4
  gigs, a decisão sobre o que fazer com eles e o aviso de que as métricas a `0`
  no painel são tautológicas (filtro de 30 dias aplicado a gigs pausados).
- Decisão: manter os 4 pausados (invisíveis ao comprador, logo inofensivos; e
  apagar é irreversível) e criar gigs novos — no Gig 1 mudaria 100% do
  conteúdo, e com 0 encomendas e 0 reviews não há equidade acumulada a
  preservar.

## Sessao 20:37
