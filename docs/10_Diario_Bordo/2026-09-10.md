# Diario de Bordo — Portfolio

**Data:** 2026-09-10

## Sessao 19:41

### Trabalho feito
20:26 — Continuação da preparação do perfil Fiverr (`docs/50_Fiverr/Perfil_e_Gigs.md`).

- **About me:** a contagem antiga (~590) estava errada — o texto real tinha
  635 caracteres, acima do limite de 600. Reduzido para 588, com margem de
  segurança.
- **Portfolio reescrito** a partir da secção "Marcos do trabalho técnico" do
  site: 4 entradas (Operação Floresta Segura, PIGE, SEPNA, CIPO), usando as 6
  imagens já existentes em `assets/diagrama-*.png` e texto adaptado de
  `en/index.html`. Saem as 4 entradas antigas que vinham de outras secções do
  site (GTO, ArcGIS Reports, CAPVI, PoF) — misturavam a fonte e não
  correspondiam aos "Marcos" indicados pelo utilizador.
- **Decisão tomada:** nomear GNR, UEPS, SEPNA e CIPO abertamente no perfil
  comercial, já que o portefólio público já o faz sem reservas. Aplicada
  também à Work Experience (secção 8), que deixa de ter duas versões
  (com/sem nome da entidade) e passa a usar directamente "Guarda Nacional
  Republicana (GNR)".
- Checklist de execução (secção 9) actualizado: as duas decisões resolvidas
  hoje marcadas `[x]`; os restantes itens continuam pendentes de acção manual
  do utilizador no painel da Fiverr.
- `docs/tasks.md` actualizado com o histórico desta sessão.

21:15 — Correcção dos campos do Portfolio a partir de captura real do
formulário da Fiverr (o utilizador já tinha começado a preencher a entrada 1
no painel).

- Descoberto: Project name tem limite de **50 caracteres** (os títulos
  escritos antes tinham 60-75 — teriam sido recusados no painel); Project
  description tem limite de **1400** (bem mais espaço do que os ~400-460
  usados antes, por falta de confirmação do limite real); e existem 3 campos
  obrigatórios/adicionais não previstos: **Industry** (dropdown fechado, com
  dezenas de opções), **Project duration** (dropdown) e **Project cost**
  (obrigatório, valor em $).
- Títulos das 4 entradas encurtados para caber no limite de 50.
- Descrições reescritas para usar o espaço até 1400 caracteres — formato
  client / goal / my role / what I built / result — com mais detalhe técnico
  de cada marco, extraído de `en/index.html`.
- **Project cost:** como os 4 projectos foram feitos como funcionário da GNR
  (não há factura real), defini uma estimativa de valor de mercado por
  projecto (freelancer/agência sénior GIS+Python, ~€45-60/h, ajustado à
  complexidade), documentada explicitamente como estimativa no ficheiro.
- **Project started on:** os Marcos do site só têm o ano; assumi Janeiro por
  defeito (Fevereiro para o CIPO, cuja tempestade foi "início de 2026") —
  fica marcado para o utilizador confirmar o mês real se o souber.
- `docs/tasks.md` actualizado com o histórico desta segunda correcção.

### Fim da sessão
20:57 — Sessão encerrada a pedido do utilizador.

**Por fazer, quando retomar:**
- Confirmar no dropdown "Industry" do painel quais das categorias sugeridas
  existem (secção 7 do documento tem as sugestões por entrada)
- Confirmar/corrigir o mês real de início de cada um dos 4 projectos (assumi
  Janeiro por defeito)
- Verificar `isActivationCompleted: false` no painel de vendedor
- Corrigir campo de idioma para `Fluent`
- Colar no painel: About me, skills, Gig 1, as 4 entradas de Portfolio, as 2
  de Work Experience
